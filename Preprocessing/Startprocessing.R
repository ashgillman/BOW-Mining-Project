# Preprocessing step will convert to modual at some point in the future.
# Text mining package I think

exe <- "C:/Program Files/xpdf64/pdftotext.exe"
path <- "E:/TestData/1ch speech NMF.pdf"

require(tm)

file <- readPDF(path)
# Inputfile <- system(paste(exe,path, wait=FALSE))

print(Inputfile)
system

